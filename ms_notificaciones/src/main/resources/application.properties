# ── Base de datos ────────────────────────────────────────────────────────────
DB_URL=jdbc:postgresql://localhost:5432/ms_notificaciones
DB_USER=postgres
DB_PASS=postgres

# ── Servidor HTTP ─────────────────────────────────────────────────────────────
SERVER_PORT=8084

# ── SMTP (Gmail como ejemplo; se puede cambiar a cualquier proveedor) ─────────
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USER=biblioteca@uagrm.bo
MAIL_PASS=tu_contrasena_app
MAIL_FROM=Biblioteca UAGRM <biblioteca@uagrm.bo>

# ── Comportamiento del servicio ───────────────────────────────────────────────
# REAL  → envía correo de verdad via SMTP
# MOCK  → solo imprime en consola (útil en desarrollo)
MAIL_MODE=MOCK